#include<stdio.h>
#include<conio.h>
void main()
{
    int age = 18;//eligible to vote
    if(age>=18)
    {
        printf("eligible to vote");
    }
    else
    {
        printf("can't vote");
    }
    getch();
}